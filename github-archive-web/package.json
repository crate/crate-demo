{
  "name": "crate.demo.github",
  "description": "Analyzing GitHub Archive data with Crate",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server app -a localhost -p 8000 -c-1 "
  }
}
