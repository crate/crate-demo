version: '2'

services:
  spark-master:
    container_name: spark-1
    hostname: demo-07
    extends:
      service: spark-worker
      file: services.inc.yml
    environment:
      SPARK_LOCAL_IP: 10.81.0.12
