{
  "name": "github-archive-demo",
  "version": "0.0.1",
  "description": "Crate demo for visualizing data from githubarchive.org",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node_modules/http-server/bin/http-server",
    "postinstall": "ln -s node_modules/bower/bin/bower ./bower && chmod +x ./bower",
    "prestart": "./bower install"
  },
  "author": "Crate IO",
  "devDependencies": {
    "bower": "^1.5.2",
    "http-server": "^0.8.0"
  }
}
